environment:
  global:
    CHANS_DEV: "-c pyviz"
  matrix:
    - PY: "3.6"
      PYTHON: "C:\\Python36-x64"
      CONDA: "C:\\Miniconda36-x64"

install:
  - "SET PATH=%CONDA%;%CONDA%\\Scripts;%PATH%"
  - "conda install -y %CHANS% pyctdev && doit ecosystem_setup"
  - "doit env_create --name=contrib_colormaps --python=%PY%"
  - "activate contrib_colormaps"
  - "doit develop_install %CHANS% -o examples"
  - "doit env_capture"

build: off

test_script:
  - "doit test_all"
